name: CI - Build and Validate Frontend

on:
  push:
    branches:
      - develop
      - release
      - production
  pull_request:
    branches:
      - develop
      - release
      - production

jobs:
  frontend-ci:
    name: HTML CSS Validation and Docker Build
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4

      - name: Install HTML and CSS Linters
        run: |
          npm install -g htmlhint stylelint stylelint-config-standard

      - name: Validate HTML
        run: |
          htmlhint src/frontend/index.html

      - name: Create Stylelint Config
        run: |
          echo '{ "extends": "stylelint-config-standard" }' > .stylelintrc.json

      - name: Validate CSS
        run: |
          stylelint "src/frontend/**/*.css"

      - name: Build Docker Image
        run: |
          docker build -t cloudshop-frontend:ci .